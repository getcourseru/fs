export function parseURL() {
    //const [packageKind, teacherKind, lessonDuration, lessonCount] = String(window.location.href.match(/\w+\/\w+\/\d+\/\d+/)).split('/');
    //const [packageKind, teacherKind, lessonDuration, lessonCount] = [`group`, `ru`, 60, 24]
    /*
        packageKind = pack, abon, group
        teacherKind = ru, en
        lessonDuration = 30, 45, 60, 90
        lessonCount = 5, 10, 15, 30, 60, 90 || 8, 12, 16, 24, 36, 48
    */

    console.log('Импорт работает')
}
